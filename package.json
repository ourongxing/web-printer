{
  "name": "@web-printer/monorepo",
  "type": "module",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter=@web-printer/* run build",
    "test": "pnpm --filter=test run dev"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^18.11.11",
    "fs-extra": "^11.1.0",
    "playwright": "^1.28.1",
    "tsup": "^6.5.0"
  }
}